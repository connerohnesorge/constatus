[package]
name = "constatus"
version = "0.2.0"
edition = "2024"
description = "A configurable status line for Claude Code"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
clap = { version = "4", features = ["derive"] }
